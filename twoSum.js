/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
  const hashTable = {};

  for(let i = 0; i <= nums.length; i++) {
    fistNum = target - nums[i];
    if(hashTable[]:) {
    }else {
    }
  }
};
